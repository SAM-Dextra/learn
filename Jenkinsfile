pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the app'
            }
        }
        stage('Test') {
            steps {
                echo 'Running Tests'
            }
        }
        stage('Deploying') {
            steps {
                echo 'Deploying the app'
            }
        }
    }
}
